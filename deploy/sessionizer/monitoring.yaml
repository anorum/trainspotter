---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sessionizer
  namespace: blockade
spec:
  selector:
    matchLabels:
      app: sessionizer
  endpoints:
    - port: metrics
      interval: 30s
