---
import SessionLog from "../components/SessionLog";
import Page from "../layouts/Page.astro";
import { featuredLabels } from "../lib/crossings";
---

<Page
  title="Train sheet - PDX Train"
  description={`Every recorded train blockage at SE ${featuredLabels()}, with the camera frames that saw it.`}
  active="log"
>
  <SessionLog client:load />
</Page>
