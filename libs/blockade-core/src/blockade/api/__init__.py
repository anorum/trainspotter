"""Serving-layer logic: response models and the live-state reducer.

Logic lives here, plain and unit-tested; the FastAPI service in services/api
is a thin shell around it - the same split as the detector and the sessionizer.
"""
